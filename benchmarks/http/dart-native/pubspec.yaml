name: bench_http_dart_native
description: >
  Phase 3 HTTP server (Shelf) backed by dart_db_connector (FFI over
  libpq) — the connector in this repository.
publish_to: none

environment:
  sdk: ^3.4.0

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  dart_db_connector:
    path: ../../../packages/dart_db_connector
